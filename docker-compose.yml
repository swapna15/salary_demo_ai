services:
  neo4j:
    image: neo4j:5-community
    ports:
      - "7474:7474" # browser UI: http://localhost:7474
      - "7687:7687" # bolt (used by agent.py)
    environment:
      - NEO4J_AUTH=neo4j/salarydemo123
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
